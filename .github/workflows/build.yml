name: Build testcases and library

on: push
  branches:
    - '*'
    - '!master'

jobs:
  build_testcases:
    uses: ./.github/workflows/assemble_testcases.yml
  # download_testcases:
  #   runs-on: ubuntu-latest
  #   needs: build_testcases
  #   steps:
  #     - uses: actions/download-artifact@v4
  #       with:
  #         name: testcases
