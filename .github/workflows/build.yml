name: Build testcases and library

on: push

jobs:
  build_testcases:
    uses: silagokth/drra-component-lib/.github/workflows/assemble_testcases.yml@5-release-package
  download_testcases:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: testcases
